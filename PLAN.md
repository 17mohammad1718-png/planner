# پلن کامل اپ Planner

> تاریخ: ۱۴۰۴/۰۶/۰۷ — وضعیت: پلن اولیه، در انتظار پیاده‌سازی

## ۱. چشم‌انداز
اپ اندرویدی یکپارچه برای ۴ نیاز اصلی:
1. **ایده‌ها (Ideas)** — جرقه‌ها رو سریع ثبت کن، بعداً پخته‌شون کن
2. **پروژه‌ها (Projects)** — هر ایده که جدی شد → تبدیل به پروژه با تسک و ددلاین
3. **عادت‌های روزانه (Habits)** — کارهای تکراری (ورزش، مطالعه، مدیتیشن) با تیک روزانه و استریک
4. **کارهای روزانه (Tasks)** — تسک‌های یک‌باره یا تکراری با تاریخ و اولویت

همه این‌ها روی **تقویم دوگانه شمسی/میلادی** قابل مشاهده و مدیریت باشن.

---

## ۲. معماری پیشنهادی

### گزینه A (پیشنهاد اصلی): Expo + React Native + TypeScript
- **چرا:** سریع‌ترین راه به APK، هم‌خانواده React، کتابخونه شمسی آماده، بدون Android Studio سنگین هم build میشه
- **دیتابیس لوکال:** `expo-sqlite` (یا `expo-sqlite` + `drizzle-orm`)
- **State:** `zustand`
- **تقویم:** `jalaali-js` برای تبدیل تاریخ + کامپوننت تقویم سفارشی (ماه‌نما شمسی/میلادی با سوییچ)
- **UI:** `nativewind` (Tailwind برای React Native)

### گزینه B: Kotlin + Jetpack Compose (Native)
- پرفورمنس و دسترسی نیتیو بهتر، ولی زمان ساخت ۲-۳ برابر و نیاز به Android Studio + Kotlin

> **پیش‌فرض پلن = گزینه A** مگر خلافش خواسته شود.

---

## ۳. اسکوپ

### هست (IN) — فاز ۱ (MVP)
| ماژول | قابلیت |
|-------|--------|
| 💡 ایده | عنوان، توضیح، تگ، وضعیت (خام/در بررسی/شروع شده/آرشیو)، تاریخ ایجاد |
| 📁 پروژه | عنوان، توضیح، ددلاین، تسک‌های زیرمجموعه، درصد پیشرفت، وضعیت |
| 🔁 عادت | عنوان، آیکون/رنگ، تیک روزانه، استریک (روزهای متوالی)، تاریخچه |
| ✅ تسک روزانه | عنوان، توضیح، تاریخ+ساعت، تکرار (روزانه/هفتگی/سفارشی)، اولویت، انجام شد/نشد |
| 📅 تقویم | ماه‌نمای شمسی/میلادی (سوییچ)، نمایش نقطه/رنگ برای روزهای دارای آیتم، کلیک روی روز = لیست کارهای آن روز، ناوبری ماه/سال |

### نیست (OUT) — فاز ۲ به بعد
- لاگین / کلاود سینک / بکاپ آنلاین (فعلا فقط لوکال)
- نوتیفیکیشن و یادآور
- اشتراک‌گذاری تیمی
- نسخه iOS / وب
- ویجت هوم‌اسکرین

---

## ۴. مدل داده (SQLite)

```sql
ideas(id, title, description, tags, status, created_at, updated_at)
projects(id, title, description, deadline, progress, status, created_at)
project_tasks(id, project_id, title, done, created_at)
habits(id, title, icon, color, created_at)
habit_logs(id, habit_id, date, done) -- هر روز یک ردیف
tasks(id, title, description, date, time, repeat, priority, done, created_at)
```

---

## ۵. نقشه تسک‌ها (هر تسک = ۲ تا ۵ دقیقه + قابل تست)

### فاز ۰ — اسکلت
- [ ] **T0-1** `planner/` — ساخت پروژه Expo: `npx create-expo-app planner --template` + TypeScript + NativeWind → تست `npx expo start`
- [ ] **T0-2** `src/db/schema.ts` — طراحی اسکیما SQLite (جدول‌های بالا) + تابع `initDB()`
- [ ] **T0-3** `src/lib/jalali.ts` — نصب `jalaali-js` + توابع `toJalali()`, `toGregorian()`, `formatJalali()`, `formatGregorian()`

### فاز ۱ — هسته
- [ ] **T1-1** `src/screens/IdeasScreen.tsx` — لیست + افزودن/ویرایش/تغییر وضعیت ایده
- [ ] **T1-2** `src/screens/ProjectsScreen.tsx` — لیست پروژه + تسک‌های داخلی + نوار پیشرفت
- [ ] **T1-3** `src/screens/HabitsScreen.tsx` — افزودن عادت + گرید تیک روزانه + محاسبه استریک
- [ ] **T1-4** `src/screens/TasksScreen.tsx` — افزودن تسک با DatePicker + اولویت + تکرار
- [ ] **T1-5** `src/screens/CalendarScreen.tsx` — تقویم ماهانه (سوییچ شمسی/میلادی)، نقطه‌گذاری روزها، کلیک = لیست آیتم‌های روز

### فاز ۲ — پولیش
- [ ] **T2-1** `src/components/BottomTabs.tsx` — تب‌بندی ۵ تایی (ایده/پروژه/خانه/عادت/تقویم)
- [ ] **T2-2** `src/lib/seed.ts` — داده نمونه برای تست
- [ ] **T2-3** بیلد APK: `eas build -p android --profile preview` → فایل قابل نصب

> هر تسک بعد از اتمام با `npx expo start` روی گوشی/امیولاتور تست می‌شود.

---

## ۶. ریسک‌ها و راه‌حل

| ریسک | توضیح | راه‌حل |
|------|-------|--------|
| تقویم شمسی دقیق | کبیسه شمسی گاهی اشتباه محاسبه میشه | استفاده از `jalaali-js` تست‌شده + تست دستی سال ۱۴۰۳/۱۴۰۴ |
| حجم APK | Expo اولیه ۴۰-۵۰ مگ | بعداً `expo prebuild` + بیلد bare برای سبک‌سازی |
| اسکوپ بزرگ MVP | ۴ ماژول همزمان | فازبندی بالا — اول هسته، بعد پولیش |

---

## ۷. تصمیم‌های باز (قبل از شروع کد)
1. تکنولوژی نهایی: Expo یا Kotlin؟
2. زبان اپ: فارسی کامل یا دوزبانه؟
3. دیتا فقط لوکال بمونه یا از اول سینک ابری (Supabase/Firebase)؟
4. اولویت شروع: پیشنهاد = تقویم + تسک → عادت → ایده/پروژه

---

## ۸. لینک‌ها
- ریپو: https://github.com/17mohammad1718-png/planner
- Jalali lib: https://github.com/jalaali/jalaali-js
- Expo docs: https://docs.expo.dev/
